apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: example-nginx
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: gsd
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: gsd
            port:
              number: 3000
