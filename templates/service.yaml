apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-apps.fullname" . }}
  labels:
    {{- include "argo-apps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argo-apps.selectorLabels" . | nindent 4 }}
